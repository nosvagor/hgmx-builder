package nav

templ (p *Props) NavbarBookmarks() {
	if p.Bookmarks != nil {
		<div
			id="navbar-bookmarks"
			class="flex gap-1 items-center font-medium font-display pl-1"
			hx-target="#main"
			hx-swap="outerHTML"
			hx-push-url="true"
		>
			for i, link := range p.Bookmarks {
				if i >= 4 {
					{{ break }}
				}
				@link
			}
		</div>
	}
}

templ (p *Props) MenuBookmarks() {
	if p.Bookmarks != nil {
		<div
			id="menu-bookmarks"
			class="flex flex-col gap-4"
			hx-target="#main"
			hx-swap="outerHTML"
			hx-push-url="true"
		>
			@p.Logo
			for _, link := range p.Bookmarks {
				@link
			}
		</div>
	}
}
